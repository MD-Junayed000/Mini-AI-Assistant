# Optimized forCPU-only,stable resolver


# === Core FastAPI / Server ===
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
httpx==0.27.2
tenacity==9.0.0
structlog==24.4.0
prometheus-client==0.21.0
slowapi==0.1.9
limits==3.13.0

# === Memory / DB ===
motor==3.6.0
pymongo==4.9.2

# === Observability (optional) ===
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0

# === Document Parsing ===
docling==2.18.0
rapidocr-onnxruntime==1.4.4
pdfplumber==0.11.4

# === LLM Clients ===
openai==1.55.0

# === BM25 + Reranking ===
rank-bm25==0.2.2


# === HEAVY ML / VECTOR STORE DEPS (CRITICAL ORDER) ===


# Base numerical & tokenization libraries
numpy==2.0.2
tokenizers<0.21

# PyTorch CPU-only wheels (prevents CUDA bloat)
torch==2.4.1+cpu
torchvision==0.19.1+cpu
torchaudio==2.4.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Sentence Transformers (must come after torch)
sentence-transformers==3.4.1

# ChromaDB (after sentence-transformers)
chromadb>=1.1.0,<2.0

# === Tests ===
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-httpx==0.32.0
respx==0.21.1